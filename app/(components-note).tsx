// Color system used (4 total):
// - Primary: Blue (bg-blue-600 / text-blue-600)
// - Neutrals: background/foreground tokens (dark by default), Border
// - Accent: Green (for small badges)
// Typography: default system font via font-sans classes.
// Layout: mobile-first, uses flex/grid with gaps, no gradients.
export {}
